import 'package:flutter_test/flutter_test.dart';
import 'package:snapquest/snapquest_app.dart';
import 'zz_audit_shot_support.dart';

void main() {
  setUpAll(loadFonts);
  testWidgets('audit shots', (tester) async {
    phone(tester);
    await tester.pumpWidget(framed(const SnapQuestApp()));
    await shot(tester, 'snapquest-01-home');
    await tapText(tester, 'Red pebble');
    await shot(tester, 'snapquest-02-caught');
    dumpTexts(tester, 'caught');
  });
}
