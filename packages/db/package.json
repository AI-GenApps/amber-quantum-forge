{
  "name": "@repo/db",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun scripts/seed-app-config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/minimatch": "^6.0.0",
    "@types/node": "^20.10.6",
    "drizzle-kit": "^0.31.5",
    "dotenv": "^16.4.7",
    "typescript": "5.5.4"
  }
}
