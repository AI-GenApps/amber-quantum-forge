# Verification report and unresolved blockers

The gaming preparation is reported in five states: specified, implemented, integrated, verified, and enabled. A foundation check or compiled scaffold does not advance a capability to enabled. The requirement ledgers in the five handoffs are the source of truth for each ID.

The current copy-review criterion is that primary game screens use concise game-native language. Implementation and status explanations belong in internal documentation or secondary help. The wording review and screenshot pair status are recorded in [the visual review](visual-review.md); copy acceptance is separate from physical-device and Play readiness.

## Historical checks from the frozen worktree

- `PATH=/tmp/luna-bun-1.3.3/bin:$PATH bun install --frozen-lockfile` completed with the pinned Bun release. The official `bun-v1.3.3` Darwin arm64 archive matched SHA-256 `f50f5cc767c3882c46675fbe07e0b7b1df71a73ce544aadb537ad9261af00bb1`.
- `PATH=/tmp/luna-bun-1.3.3/bin:$PATH bun run games:doctor` reports Bun 1.3.3, Flutter 3.47.3, Dart 3.13.3, Xcode 26.2, and ADB available. The same pinned command completed `games:bootstrap`, including frozen Bun installation and all scoped Pub/Flutter dependency resolutions.
- `bun run games:format:check`, `bun run games:analyze`, `bun run games:test`, `bun run games:validate:strict`, and the pinned manual `bun run games:parity` pass. The final gaming source check recorded 100 passing game tests across 11 scoped invocations: pure rule packages `5 + 5 + 10 + 8 + 3`, `platform_core` 24, and client suites Merge Relay 5, Pocket Biome 8, Sixty-Second Heist 8, Meme Court 5 and SnapQuest 19. The authoritative consolidated log is `/tmp/luna-final-games-test.log`. The parity fixture compares the checked-in Merge Relay Dart VM and compiled-JS replay output, including the portable high-value integer limit.
- The pinned serial tooling run recorded 39 pass and 0 fail, including registry/config/content validation, affected detection, generator isolation, metadata, CLI behavior, codegen formatter stability, and replay parity. The earlier parallel run is not used as acceptance evidence because its child-process PATH inherited a different Bun binary.
- `bun run check:ci` and `bun run knip:ci` pass. In a source-only clean checkout, the first `bun run typecheck` reports the existing `apps/web/app/page.tsx` `@repo/ui` dist prerequisite; `bun run --filter @repo/ui build` followed by `bun run typecheck` passes all 11 workspaces. The API game boundary suite passes 18 tests with `API_JWT_SECRET` set to a test-only value; the full API test command remains the repository-level gate.
- `bun run test` passes the existing API suite: 10 files and 43 tests. `bun run check:doc-paths`, staged-doc checks, and both Mintlify validation commands pass.
- A clean source copy at `/tmp/luna-clean-source-snapshot-final-20260917`, made from tracked and intended untracked files before validation outputs, installed with pinned Bun 1.3.3, completed `games:bootstrap`, passed strict validation, and regenerated the Dart registry without a hash change (`8720ea0a771c957da02eb211af528ab20e36aaffc09556115166cfe31901e484`). This proves the documented setup does not depend on ignored build output, Drive access, or a developer's existing package cache.
- The final source-owner checks also passed the gaming actionlint, strict/content/icon/parity gates, generated-sixth-app validation/analyze/widget checks, and codegen stability. The existing full-repository `.github/workflows/preview.yml:41` failure remains preserved as a pre-existing root check and is not converted into a gaming pass.
- Five independent Flutter clients and six scoped Dart packages have separate analysis/test commands. The first-three clients also have restart, write-failure, future-schema, and corrupt-payload recovery widget tests.
- XcodeGen source remains in each app's `ios/project.yml`; generated native projects are regenerated from source. Five Android debug APKs and five unsigned iOS artifacts were built during the native integration phase. Those artifacts are ignored and are not release submissions.
- Native identity/content/permission validation passes. Merge Relay, Pocket Biome, Sixty-Second Heist, and Meme Court have no camera permission or camera SDK. SnapQuest alone declares camera access, pins `camera 0.12.1`, uses Android API 24 as its minimum, uses iOS 15.0, and sets `enable-swift-package-manager: false` so the plugin is integrated through CocoaPods. This app-local choice must be rerun through the camera lifecycle matrix if the SDK integration changes.
- SnapQuest denial, desk fallback, bounded camera metadata, cancellation, background, disposal, and raw-buffer clearing tests pass. No descriptor classifier, camera-quality claim, or physical-recognition result is marked verified.
- The final Court check has 5 focused widget tests covering the authored prompt and caption choices, selected phrase IDs, relaunch, winner caption, legacy flow compatibility, frozen-round restore, malformed-save rollback and reset; `flutter analyze` reports no issues. SnapQuest has 19 scoped tests covering its desk hunt, wrong-object feedback, next-target reveal, local album restore, camera lifecycle fixtures, wrong-descriptor rejection, busy capture gating, corrupt-save recovery, and the distinction between a received frame and unverified recognition; `flutter analyze` reports no issues for both apps.
- Court and SnapQuest inject `MemorySaveStore` in widget tests and now use app-scoped `path_provider` application-documents adapters in their local entrypoints. A one-time migration reads the legacy app namespace from the former temporary root only when the destination is absent, verifies destination readback, and deletes only that migrated source save; source, destination and readback failures preserve the old data. A real-file migration suite also keeps a validated legacy fallback readable when the destination is corrupt or transiently unreadable, refuses to overwrite the unreadable destination, and verifies foreign app scopes stay isolated. The final Android QA batch rebuilt, installed and relaunched both clients with these declarations; this accepts the bounded migration smoke evidence while the full MVP durability and service gates remain open.
- The generic sixth-app generator was run in a disposable directory; generated identity/config/native boundaries, analysis, and widget checks pass without importing a registered game or rules package.

## Current source and artifact record

Final QA production metadata for all five apps names commit `30b3057f166883d65da4107cd9801166d55aab0c`, a dirty worktree, and shared source `023c03ee010ad5b449024de3bdeec81157dd0b64dc5be56a36b94ae20cced167`. The per-app source, content, rules and artifact records are:

| App | Production artifact ID | App source / content / rules SHA-256 | APK SHA-256 | AAB SHA-256 |
|---|---|---|---|---|
| Merge Relay | `merge_relay-android-production-0.1.0+1` | `a73f41e13cda0f00a44e8c605a0460b522122eed3e0a0a97a037955698e6a4c4` / `31380040b1c67b6d45ff6535d881bbb6854eff1578c1cd334f8b260404ef202d` / `7e45c86866633c13e6cfe1e99492b99cefb8d1f322ea04998c571ec2d3c2cd61` | `3c8bb71bde67b602a5bf78fd6546ff4884ea922df3c090a2cb44c7486ceb2956` | `36fe4512e99f741963aae1887a5c5a19b36ce4cc78751ca84099185189fb2f3c` |
| Pocket Biome | `pocket_biome-android-production-0.1.0+1` | `dcaffc6ebdd8c566be163e2d4aa66ca47c55dfdd22a307493db8563bc1f80d35` / `0ed5f3fad82f07a891edb144e0bc2e608d9427958166c1fa21a0f787e63bb6e0` / `6572a74bed4e36e72347337e2b93bb2088429c36930fc643486e85ee8af8f782` | `bd517a39269a183a01188b3e2c1c0f4485f9470effe5161b85b466946df033b3` | `e5e58484a53a1bf991f69b32946e345f0d19d49e523b35913e09dbee9595216b` |
| Sixty-Second Heist | `sixty_second_heist-android-production-0.1.0+1` | `6037b8df81bfbd116503cf210b0a04b60af1e4dbc1ac45b47dd0c122e6e6152d` / `64311a8a1d90b7e4b604d227a1d3c6a2e22b27befa6a57b4f17064b2aa6318cb` / `9292f66f6f2ab85b492a0525358895495bdb0b1a6a1aac901f37857493bd1ad1` | `84dcd681a09902dbe587f8e7a5ab885574f7502c9c3767374561bda0a701c1d8` | `75de69e566933ba79c0d6502de2de49e667cb0c964749851a195ed6c9a800b24` |
| Meme Court | `meme_court-android-production-0.1.0+1` | `12eff4e8c9e5d42925f3809f095464f7785a6831cab4344c560f867bf63d87db` / `0a5b4bd28c5b924fa6793e1a072cf966ebfca9e9169cf02148a7d78a3f08fbcb` / `a2b5d311a8fce0616aff917974d544b250f049a6671a5b7429c6db8649d1a39b` | `c57fce059a9aec3122fe120217b6d2ad87b2baf9784fe02c9cedde262ae02872` | `446949938550456e1cdc1bfd6410346c9cc567b2ff853476a4feee1da89de307` |
| SnapQuest | `snapquest-android-production-0.1.0+1` | `5c1d5dbfb33781d62f12e4f299c1e43943fc7099c8e1b351a5a39345eda0ae90` / `ed788c518344688f0fc059d143edf500c4ddab36bdaf7dc277c20989d746689d` / `27e4ffde6801e7719f424d6cf745cc4cdeb2bef724ef5d43eb2c1de5602e4621` | `e11e7f4d2ecc5474b4de0fd25c299e4c3aa1ac2baf42316265b1520f9fcd723a` | `b68f1d99861e4ef35bc15ed0e10404f02bec5b5a483408a4438345430c915028` |

QA's final artifact pass rebuilt, installed and relaunched all five apps after migration. The migration helper is unused/tree-shaken in the first three, so unchanged APK hashes are expected and do not indicate stale builds. The QA ledger inputs are `/private/tmp/amber-android-final-metadata-{merge_relay,pocket_biome,sixty_second_heist,meme_court,snapquest}-production.json`, `/private/tmp/amber-android-final-aab-build-{app}.log`, `/private/tmp/amber-android-final-aab-verification.log`, and `/private/tmp/amber-android-final-aab-manifest-summary.log`; installed APKs are `/private/tmp/amber-installed-{app}.apk`. The final production AABs use the local `NOTFORUPLOAD` test key; `jarsigner` and `zipalign` verification passed with the recorded self-signed/expiry/no-timestamp warnings. The manifest-summary helper's `NoSuchFileException: /AndroidManifest.xml` is retained as historical tooling output; read-only decoding of each frozen AAB's nested `base/manifest/AndroidManifest.xml` confirmed the production package ID, version `0.1.0`/code `1`, min SDK `24` and target SDK `36`, as recorded in the [durable provenance ledger](evidence/final-android-provenance-ledger). The exact screenshot/device/route joins are recorded in the visual review; no Play upload artifact is claimed.

## Current bounded rechecks

- The first-three lifecycle/persistence/accessibility and copy refinement is present in the current worktree. Final scoped checks pass for Merge Relay (5 checks), Pocket Biome (8 checks) and Sixty-Second Heist (8 checks); focused `flutter analyze` passes for all three. These are bounded post-fix checks, not a replacement for the clean-worktree integration gate.
- The latest Android run produced readable 1080×2400 final smoke relaunch captures for all five apps, accepted by Android QA as postmigration physical evidence on `SM-A525F` serial `RZ8R32EAB7T`, Android 14. The visual review records the per-app artifact, source and route joins. This closes the bounded physical visual milestone, while Play readiness and the 79 full-MVP rows remain separate.
- Court's source caption-flow remediation is complete. SnapQuest's lifecycle ordering and capture gating remediation is covered by the passing 19-test suite; the accepted smoke camera capture remains `frameCaptured=true` with `descriptorId=null`, so camera recognition and descriptor calibration remain unverified.
- The older durable screenshot copies are 243×540 historical references and are too small for final caption/control readability review. The accepted smoke copies preserve 1080×2400 dimensions and were copied losslessly. The visual review records the exact screenshot, artifact, source, device and route joins for the completed bounded visual milestone.

Build logs are retained outside the repository under `/tmp/luna-game-builds/` for the current local audit only. Builds and tests do not require Drive snapshots, those logs, or any temporary directory.

## Explicitly not run or blocked

- The retained native-owner baseline reports all five signed iOS artifacts installed, launched, and played. This is device baseline evidence, not distribution verification; further iOS QA is paused until after Google Play publication. No simulator or emulator result is accepted as a substitute for a device gate.
- Android physical install/launch evidence is represented by the latest app-only final smoke captures, accepted by QA on physical `SM-A525F` serial `RZ8R32EAB7T` running Android 14. QA's postmigration rebuild/install/relaunch pass covers all five apps; the first-three APK hashes remain unchanged because the migration helper is unused/tree-shaken there. SnapQuest's camera frame is captured on that run, but `descriptorId=null` and descriptor recognition remain pending. The earlier `DEVICE_NOT_FOUND` attempt is superseded and is not a current device-state instruction.
- The current-source metadata and exact artifact/capture hashes are recorded in the visual review. The accepted postmigration smoke set closes the bounded visual-review task; Play gates and MVP ledgers remain separate, and SnapQuest descriptor calibration remains open.
- The effective release sequence is Google Play/Android first, following user steering on 2026-09-17. Play readiness means artifacts, native permissions, listing/privacy/signing checks, device evidence and support gates are prepared; it does not mean external registration, submission or publication. Store submission, spending and production migrations remain disabled.
- `bun run build` reaches the web production build and then stops at page-data collection because the existing web AI module requires `OPENAI_API_KEY`; this is recorded as an environment baseline blocker, not a gaming regression. A direct `apps/web` build passes with nonsecret local placeholders for `OPENAI_API_KEY`, `API_JWT_SECRET`, and an invalid localhost `DATABASE_URL`; no AI request or external distribution action was run.
- SnapQuest held-out descriptor thresholds, orientation/format coverage, latency, and real camera recognition remain unverified. Only the equal desk path and capability denial/failure behavior are enabled in local foundation evidence.
- External store registration, publisher credentials, product IDs, purchases, advertising, distribution signing, release submission, production migrations, and vendor-term acceptance remain disabled or unresolved.
- Full v0.3 MVP completion is not claimed. The five handoffs map every requirement to an owner, contract, current evidence, and remaining tests; foundation smoke paths are not substitutes for those MVPs.

The existing non-game baseline remains separately attributable. If a root build is blocked by an environment requirement such as `OPENAI_API_KEY`, record that exact pre-existing error beside the command result rather than converting it into a gaming pass or regression.

## Remaining Play and MVP gates

- **Android final QA owner:** retain the accepted readable smoke captures and per-app route ledger; SnapQuest still needs descriptor calibration and held-out camera evidence. The test gate is the `games:build`/`games:run` route in [the visual-review task](../../tasks/epics/13-gaming-portfolio-preparation/13-visual-review.md).
- **Tooling/integration owner:** after root and app changes freeze, run the pinned Bun bootstrap, format, analysis, test, strict validation, parity, docs-path and Mintlify checks from [the staging plan](integration-staging-plan.md). Do not call the current bounded checks a final clean-checkout result.
- **Native/release owner:** produce store-appropriate Android artifacts, verify bundle/application IDs, target and minimum SDKs, signing, privacy/data-safety declarations, content rating, listing assets, support contact, release-track configuration and rollback evidence. A debug APK or release-mode debug-signed APK is not a Play submission artifact.
- **API/service owners:** implement and test the service requirements named in each handoff, including signed app scope, durable persistence, membership/invites, settlement, recovery and idempotency. The generic API isolation boundary does not close the five game MVPs.
- **Content and safety owners:** deliver the v0.3 authored quantities, review/moderation operations, reporting/blocking, support and kill switches named by the MR, PB, SH, MC and SQ ledgers. Local fixtures remain preparation evidence.
- **SnapQuest owner:** complete physical permission/lifecycle testing, descriptor calibration, held-out quality/orientation/latency/privacy evidence and safe disable behavior before any camera recognition or camera-derived reward is enabled.
- **Meme Court owner:** complete private membership/invites, moderation staffing and audit, reviewed prompt/content quantity, web voting, reporting/blocking, share withdrawal and service isolation acceptance before UGC or social features are enabled.
- **iOS QA owner:** retain the prior five-app signed/install/play baseline as history and keep further iOS QA paused until after Google Play publication.
- **Publication owner:** verify Play publisher registration, signing credentials, policy declarations, store listing, support ownership and staged submission/review checks. No external registration, submission or publication is currently claimed.
