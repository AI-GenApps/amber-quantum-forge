# iOS App — Starter

SwiftUI app generated via [XcodeGen](https://github.com/yonaskolb/XcodeGen).

## Generate Xcode project

```bash
cd apps-native/ios-app
xcodegen generate
```

Run this after any changes to `project.yml`.
