import SwiftUI

extension Color {
    static let tokenBackground = Color("Background")
    static let tokenSurface = Color("Surface")
    static let tokenPrimary = Color("Primary")
    static let tokenSecondary = Color("Secondary")
    static let tokenText = Color("Text")
    static let tokenMuted = Color("Muted")
}
