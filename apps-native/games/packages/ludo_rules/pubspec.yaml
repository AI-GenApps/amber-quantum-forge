name: ludo_rules
description: Pure Dart Ludo board, movement, turn and dice rules engine.
publish_to: none
resolution: workspace
environment:
  sdk: ">=3.11.0 <4.0.0"
dependencies:
  platform_core:
    path: ../platform_core
dev_dependencies:
  test: ^1.26.3
