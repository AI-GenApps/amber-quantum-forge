name: merge_rules
description: Pure deterministic Merge Relay board rules and replay validation.
publish_to: none
resolution: workspace
environment:
  sdk: ">=3.11.0 <4.0.0"
dependencies:
  crypto: ^3.0.7
  platform_core:
    path: ../platform_core
dev_dependencies:
  test: ^1.26.3
